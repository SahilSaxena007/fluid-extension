(function () {
  /** Activates Fluid edit mode. */
  function activate() {
    return false;
  }

  /** Deactivates Fluid edit mode. */
  function deactivate() {
    return false;
  }

  window.fluidDragEditor = {
    activate,
    deactivate
  };
}());
